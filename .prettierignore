build/
node_modules/
.wrangler/
package-lock.json

# Vendored Claude Code skills/agents — upstream files, not ours to format
.claude/

# Regenerated by the TanStack Router plugin on every build — formatting it just
# creates a diff that comes back next build. ESLint already ignores it too.
src/routeTree.gen.ts
